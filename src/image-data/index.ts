import { CourseImages } from "./course.image";
import { HomeImages } from "./home.image";
import { IconImages } from "./icon.image";
import { SharedImages } from "./shared.image";

export const IMAGES = {
  shared: SharedImages,
  home: HomeImages,
  course: CourseImages,
  icon: IconImages,
};
